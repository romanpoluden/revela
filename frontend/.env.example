# No secrets are required for demo mode.
#
# Live educational model output is enabled only when both values below are set.
# The backend URL must be browser-safe and must not contain secrets.
# VITE_REVELA_INFERENCE_BACKEND_URL="https://example-hf-space.hf.space"
# VITE_REVELA_ENABLE_LIVE_INFERENCE="true"
